name: AutoSubmit

on:
  schedule:
    - cron: '0 6,12,18 * * *'
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Auto Submit
        run: |
          sudo apt install curl -y
          sudo apt install jq -y
          ls -a
          dirs
          sudo bash submit.sh
